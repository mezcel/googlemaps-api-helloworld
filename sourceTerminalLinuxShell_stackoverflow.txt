# -----------------------------------------------
# --- Shell Script To Run Hello World Scrapy ----
# --- stackoverflow crawler -----------------------
# --- hello-worldmaps-api demo ------------------
# -----------------------------------------------

rm /home/mezcel/github/googlemaps-api-helloworld/demo-csv-json-scrape/stackoverflow_Alabama_Videmo.json
cd '/home/mezcel/github/googlemaps-api-helloworld/scrapy_virtualenv_helloworld' 
source bin/activate
ls
cd stackoverflow
scrapy crawl stackoverflow -a domain='https://stackoverflow.com/jobs?sort=p&l=Florida' -o ../../demo-csv-json-scrape/stackoverflow_Alabama_Videmo.json -t json
ls
xdg-open ../../demo-csv-json-scrape
#exit

# -----------------------------------------------
# --- finished hello-worldmaps-api demo ---------
# --- to run this script in terminal, enter: ----
# --- $ source /path dir/thisFile.txt -----------
# --- just make sure you edit th [cd] to --------
# --- reflect the virtualenv path on your Linux -
# -----------------------------------------------

<<'COMMENT'

    to run this script in terminal, enter:
    $ source /path dir/thisFile.txt
    just make sure you edit th [cd] to reflect the virtualenv path on your Linux

COMMENT
